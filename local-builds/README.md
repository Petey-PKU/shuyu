# Local build artifacts

Place locally generated APK/AAB files and temporary release packages here. Git ignores every other file in this directory; distributable builds should be attached to GitHub Releases or retained as GitHub Actions artifacts instead of committed to source control.
