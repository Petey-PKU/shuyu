# 隐私说明

书语采用 local-first（本地优先）设计。

## 保存在设备上的数据

- 用户导入的 TXT / EPUB / MOBI / AZW3 / KF8 / PDF 解析文本与本地 OCR 结果
- 书籍元数据、阅读位置与阅读统计
- 收藏的生词、释义、来源句和复习状态
- 字号、行高、阅读主题与英语朗读音色偏好
- 阅读水平测试结果、选书兴趣、想读收藏与难度反馈
- 用于本地调整推荐的单本查词次数、阅读分钟与估算阅读词数

这些数据默认不会上传到书语自有服务器。用户可在“设置 → 清除全部本地数据”中删除。

用户可在“设置 → 记录阅读统计”关闭新增的阅读分钟、连续天数和趋势记录；关闭不会影响书籍阅读进度，已保存的统计仍留在设备上，清除全部本地数据可一并删除。

## 本地词典

正式 Android / iOS 安装包的常规英文单词查询由随应用分发的 ECDICT Core 在设备本地完成，不会发送单词或书籍正文。Web 预览不加载随包 SQLite 词典，关闭在线增强时使用内置基础兜底；开启后，未收录单词才可能请求在线服务。

## 扫描版 PDF 识别

Android 版在用户确认后，可将扫描版 PDF 逐页渲染并使用随安装包提供的 Google ML Kit Latin 模型识别英文文字。PDF 页面、识别输入和识别结果在设备上处理，书语不会把这些内容上传到自有服务器或 OCR 云服务。根据 ML Kit 的隐私说明，ML Kit 不会把输入数据或识别结果发送给 Google；SDK 仍可能联系 Google 获取错误修复、模型或硬件兼容信息并发送运行指标。用户可以在识别过程中取消，未完成的结果不会加入书架。

## 在线翻译增强

在线增强默认关闭，用户可以在“设置 → 在线翻译增强”主动开启。关闭时，点词全程离线；开启时，整句翻译仍需用户在查词面板中主动点击获取。

开启时，只有用户主动点按单词并请求整句翻译，或查询未被本地词典收录的单词时，应用才可能把相关文字发送至：

- 书语翻译网关（仅在发行者配置时）：默认先请求 Microsoft Azure Translator；只有前一供应商失败时，才顺序请求腾讯云机器翻译
- 必应翻译网页服务：App 从用户设备直接取得临时网页会话并请求翻译，不使用书语发行者的 API Key；这是实验性兼容方式，不是 Microsoft 承诺稳定的开发者 API
- MyMemory：前述服务不可用时的客户端最终兜底

App 与网关均设置了超时、有限重试和进程内缓存。网关不会在应用日志中记录原文或译文；它使用原文的 SHA-256 摘要作为键，在进程内短期缓存译文和供应商名称，进程重启后缓存消失。第三方仍可能按其规则处理所选文字、用户 IP、临时 cookie、请求时间、状态码和运行指标。Microsoft、Azure、腾讯云和 MyMemory 的数据处理位置及规则不同，正式分发时应以实际部署配置更新本节并提供相应供应商隐私政策链接。

## 英语朗读

英文单词和段落朗读使用 Android 随包提供的 Piper 离线音色，或设备操作系统提供的文字转语音能力。应用只读取设备返回的可用英语音色列表，并在本地保存用户选择的音色标识；书语不把朗读文字或音频发送到自有服务器。系统或网络音色是否联网以及其数据处理方式由设备系统、语音引擎和用户安装的音色决定。

## 用户责任

用户应确保对导入内容拥有合法的阅读和处理权。本应用不会绕过 DRM，也不提供电子书下载、购买、试读、搜索或分享功能。
