# 书语产品审查与提升方向

更新时间：2026-09-13

这份审查从第一次打开应用、导入一本书、读一段正文、查词收藏、复习和换设备恢复数据的完整路径出发。目标是让用户在每一步都知道下一步做什么、数据是否保存、哪些内容会联网。

## 当前体验已经补齐的关键问题

- 首次打开会提供体验书和导入入口；空书架、搜索无结果和导入失败分别给出不同说明。
- 阅读器保存章节、段落、字符位置和阅读进度，重新打开会回到上次位置；读完后会明确显示完成状态，并提供返回书架和从头重读。
- 空白章节也会把阅读位置保存为有效的第一个段落索引，目录切换或重启不会把本地书架写成损坏状态。
- 点词卡片显示词典来源、原句和按需整句翻译；在线增强默认关闭，开启前后文案明确说明可能联网的范围。
- 在线补充释义失败时，查词卡片会明确显示已使用本地兜底，并提供单独重试入口，不把网络故障误报成“单词不存在”。
- 查词卡片提供明确的关闭按钮，用户无需依赖点击背景或系统返回即可回到正文。
- 正文可查词节点带有读屏按钮语义和操作提示，读屏用户可以发现并打开释义卡片。
- 从生词本跳回正文时，阅读器顶部返回按钮会正确标记“返回生词本”；其它入口显示“返回上一页”，不误导读屏用户。
- 设置和启动恢复弹窗声明为模态视图，读屏焦点会留在当前确认/隐私操作中，不会误读背后的页面。
- 生词带原句、来源章节和间隔复习时间；复习、收藏和本地写入失败时保留内存状态，并提供全局重试入口。
- 生词本和复习卡片会直接显示来源章节与段落，用户在多本书或长篇章节中能先确认位置再回到原文。
- 复习挖空会安全处理恢复备份中的特殊字符，异常词条不会因为正则表达式拼接导致复习页崩溃。
- 复习结果写入失败时会保留当前卡片和用户选择，卡片内可直接重试；只有设备确认保存完成后才进入下一题，避免失败结果被误报为已完成。
- 复习结果尚未保存时退出会先说明风险，并提供立即重试或稍后离开，避免用户把仍待写入的选择误认为本轮已完成。
- 生词列表和复习卡片都能直接回到保存的章节段落；书籍删除或正文损坏时会回到生词本，保留用户的学习上下文。
- 生词来源失效时，阅读器错误说明和返回按钮都会指向生词本，用户能明确知道如何选择下一个词继续学习。
- 生词本在学习中为空时提供“去读一本书/继续阅读”入口，用户不会停在只有说明文字的空状态；已掌握列表为空时仍保留安静的提示。
- 生词本支持按单词、释义、原句和书名搜索，并区分无搜索结果与尚未收藏，生词增多后仍能快速找回语境。
- 书架默认按最近打开时间排序，搜索结果也保留最近阅读优先，方便用户在多本书之间快速找回上下文。
- 首页“继续阅读”卡片同时显示当前章节位置和百分比，多本书来回切换时无需重新打开正文确认上次读到哪里。
- 书籍、进度、生词、统计和偏好支持本地备份与恢复；恢复过程有回滚和中断恢复校验。
- 首页展示近 7 天已记录的阅读分钟，区分“无记录”和“0 分钟”。旧版有日期的当日统计按原日期补入；无法补齐的历史不推测。每次记录时清理距当天超过 89 天的明细，保留累计总量；设备时钟回退时保留已有的未来日期记录，图表仅显示截至当天的 7 天。
- 设置页的英语音色列表会保留当前已选音色，即使它不在默认展示的前几项中，用户重新打开设置仍能看到选中状态。
- 设置页可关闭新增阅读统计；关闭后不再新增阅读分钟、连续天数和趋势，但阅读进度不受影响，已有统计会明确保留。
- 首页在有到期生词时提供明确的“今天可以先复习”行动卡，直接回到复习流程；没有到期词时不额外打断继续阅读。
- 首页会展示最近收藏的词、所属书籍和章节/段落位置，并能直接回到保存的原文；用户在打开应用后可以从最近语境继续学习，不必先进入生词本搜索。
- 当今天没有到期复习词但阅读目标尚未完成时，首页会给出“还差几分钟，继续阅读”的行动卡并回到当前书籍，避免用户只看到统计数字却不知道下一步。
- 当今日阅读目标已经完成且没有到期复习词时，首页仍保留“想再读几页吗？”入口，用户可以自然延续当前书籍，不会因为完成目标而失去继续阅读的路径。
- 首页最近书页会补充每本书的本地累计阅读分钟和查词次数；用户能看到自己的时间投入与查词足迹，不必只依赖全局统计猜测哪本书最有帮助。
- 生词本“学习中”会把今天到期的词置顶、按下次复习时间排列其余词；“已掌握”按最近掌握时间排列，用户进入列表后更快找到当前要处理的词。
- 阅读器“已收藏”状态与保存去重共用“书籍 ID + 单词 + 原句”判断；不同书籍的相同句子不会禁用收藏按钮。浏览器回归覆盖第二本书收藏、重新加载后的两个来源，以及回到原文后的收藏状态。
- 收藏落盘失败时，查词卡片直接提供“重试保存”；重试结果会根据所有待写入数据是否真正完成而显示成功或继续提示失败，用户无需先关闭卡片寻找全局横幅。
- 生词本标记掌握或移除时若设备写入失败，页面保留本次会话变化并显示就地“重试保存”，用户不必依赖离开页面后才出现的全局提示。
- 设置中的字号、主题、每日目标、联网增强、统计开关和音色偏好写入失败时，设置页会说明当前会话状态并提供重试，避免隐私或阅读偏好静默丢失。
- 导入正文后写入中断恢复标记失败时会自动重试一次；仍失败则清理本次正文并显示可执行的重试提示，避免书架索引失败后重启找不到刚导入的书。
- 扫描版 PDF 进入 OCR 后仍保留选中的文件名和原始计时基线，长时间识别时用户能确认正在处理哪份文件，取消状态也会持续显示。
- 恢复备份确认框会同时显示备份内容与当前设备的书籍、生词数量，并明确提示会替换当前数据，用户在覆盖前能核对范围。
- 阅读器首次操作提示会同时说明点词查义、滑动翻页、底部朗读和目录入口；读屏标签也会读出完整操作提示，减少新用户只发现部分功能的情况。
- 查词卡片会根据实际结果区分词典加载期间的在线补充释义与基础兜底，词典不可用时也不会把已返回的网络释义误报为离线结果。
- 复习完成页提供“继续阅读”和“返回生词本”两个明确去向，完成一轮后可以直接回到最近阅读或导入入口。
- 复习完成页会区分本轮“记住了”和“稍后再看”的词数，并显示下一次复习时间，用户能判断这轮学习结果和下一步安排。
- 从生词本或复习卡片进入的阅读完成页会返回生词本，直接阅读入口仍返回书架，读完一本书不会丢失原来的学习上下文。
- 首页底部隐私提示同时说明阅读进度本地保存和统计关闭入口，用户在第一次阅读前即可知道数据控制方式。
- 首页首次使用隐私提示还会明确查词次数只用于本机推荐排序、不上传，避免用户把阅读行为数据与在线翻译请求混为一谈。
- 首次开启在线翻译增强会先要求确认可能发送的文字范围；用户不确认时仍保持完全离线，关闭开关则立即生效。
- 在线翻译确认与隐私说明会同时展示当前提供方策略，用户能在开启前知道是自有网关优先还是设备直连实验性服务与兜底服务组合。
- 书架为空时首页会在顶部给出导入第一本书的行动入口，空备份或删除体验书后仍能直接开始阅读。
- 发现页取消最后一本想读时仍保留“全部”切换，空筛选状态给出回到完整推荐的路径，不会把用户困在空列表。
- 发现页会忽略旧备份或目录更新后失效的想读书目 ID，数量、筛选和书签状态只展示当前仍可用的推荐，避免出现“有想读数量但列表为空”的误导。
- 推荐页只有在近期阅读记录能与推荐目录中的书籍匹配时才显示“结合近期阅读”，让推荐依据说明与实际算法保持一致。
- 推荐依据测试覆盖匹配书目、无匹配书目和阅读量阈值，避免后续调整算法时再次出现解释与实际行为不一致。
- Web 预览的首页、空书架和推荐详情会按平台隐藏 PDF 导入承诺，并明确说明 PDF/OCR 需要正式 Android 安装包，减少用户选中文件后才发现能力不可用的落差。
- 导入和扫描 PDF 的进度提示区分总耗时与当前阶段，并明确页面文字在设备上识别、书语服务器不会收到正文；系统运行指标遵循系统服务自身规则。
- 扫描版 PDF 经过系统 OCR 确认后重新显示进度时会保留文件名，用户在长时间识别期间仍能确认正在处理的文件。
- 导入弹层会显示当前选中的文件名，用户在长时间读取、解析、保存或 OCR 时能确认正在处理的内容，减少重复选择和误判卡住。
- 导入进入保存阶段后会明确显示“正在保存…”并暂时禁用取消，避免用户误以为已经写入的书籍索引可以被撤销。
- 正文创建成功但书架索引暂时写入失败时，导入仍会进入阅读，顶部持久化提示保留最新书架并提供重试，避免用户重复导入同一个文件。
- 导入正文成功后会先记录待完成导入；如果应用在书架索引写入前被系统回收，下一次启动会检查正文是否仍在并自动补回书架，索引暂时仍写不进去时保留记录等待后续重试，减少“导入成功却找不到书”的不确定性。
- 待完成导入恢复遇到暂时的书架读取失败时会保留日志并交给正常启动重试，不会因为恢复辅助流程的一次 I/O 波动直接进入数据恢复页。
- 自动补回中断导入后会显示一次本地成功提示，用户能知道书籍为何重新出现在书架；失效标记清理或恢复重试不会制造误导性的成功通知。
- 待完成导入日志支持多本书合并，并兼容旧版单条记录；用户在上一轮索引失败后继续导入新书，重启时不会因后一条记录覆盖前一条而丢失正文。
- 清除全部本地数据会先清除旧的恢复/导入成功提示，重新初始化后不会把上一轮操作的通知误留在全新书架上。
- 阅读中收藏生词会在查词卡片内显示保存中、已加入或设备保存失败；即使本次会话可见但落盘失败，也会明确提示用户稍后重试。
- 阅读位置写入失败时，阅读器会说明当前位置只在当前会话更新并提供“重试保存”，避免用户重启后才发现进度没有落盘。
- 阅读分钟和读词数统计写入失败时，阅读器也会明确提示当前会话数据尚未落盘，并可用同一入口重试。
- 查词次数记录写入失败时同样会提示并进入重试队列，不会静默影响首页足迹和推荐排序。
- 阅读器内调整字号、行距或主题后若偏好写入失败，会保留当前会话效果并提示重试，避免用户重启后才发现排版设置没有保存。
- 查词卡片正文在小屏设备上限制最大高度并支持滚动，长原句或翻译不会把收藏、关闭和重试操作挤出屏幕。
- 长句翻译遇到 URL 或无空格文本时也会按 UTF-8 字节限制安全切分，避免单个超长片段让在线补充翻译直接失败。
- 推荐详情页收藏“想读”后会显示加入/移除结果；设备保存失败时会说明当前会话状态与后续重试方式。
- 推荐详情页收藏“想读”或提交难度反馈失败时，提示内直接提供“重试保存”，选择成功落盘后才清除失败状态。
- 发现页的快速“想读”入口保存失败时也会就地提示并提供重试，避免用户只在推荐详情页得到恢复入口。
- 发现页兴趣标签保存失败时也会明确提示并复用“重试保存”，避免视觉上的已选中状态被误认为已经跨重启保存。
- 推荐详情页的难度反馈会显示记录中状态并反馈成功或保存失败；快速点击不会并行提交，用户能确认这次判断已进入后续推荐逻辑。
- 发现页兴趣、想读筛选、等级浏览和推荐反馈控件会向读屏用户报告当前选中状态，与视觉高亮保持一致。
- 阅读器翻页、朗读和复习提交按钮会在排版等待或提交期间向读屏用户报告禁用状态，避免重复尝试。
- 快速连续点词朗读时，旧请求会在停止阶段失效，不会与最新选择重叠播放或读出过时的单词。
- 书架编辑有未保存修改时，点击背景或系统返回不会直接丢弃内容；保存中会锁定操作，保存失败会在弹层内说明原因。
- 书架编辑保存失败后，弹层会把主按钮改为“重试保存”；成功重试会清除顶部错误提示，避免用户看到已经过期的失败状态。
- 书架编辑成功只会清理自己的保存错误，不会覆盖仍需处理的导入失败提示，多个操作的恢复反馈保持独立。
- 书架导入、编辑和删除的错误来源现在彼此隔离；完成其中一个动作不会误清理另一个动作仍需处理的提示。
- 启动恢复从备份失败后，按钮会直接变为“重试恢复”；重新读取本地数据前会清理旧的恢复错误，用户能准确判断当前动作结果。
- 启动恢复切换到“清除并重新开始”时也会清理旧的备份错误，重置失败后显示的是当前操作结果。
- 设置页音色列表旁会说明自带 Android 音色与系统音色的联网边界，用户选择朗读方式时无需离开应用查找隐私信息。
- 设置页开源项目链接失败后会直接提供“重试打开”，成功尝试前会清除旧错误，用户不必重新寻找入口。
- 音色试听失败后提示会直接提供“重试试听”，并记住失败的音色；系统音色回退提示仍只说明当前实际使用的替代方案。
- 阅读器朗读失败后会记住刚才的单词或当前页，并在提示内提供“重试朗读”，用户无需重新寻找正文位置。
- 复习和生词本朗读失败后也会直接提供“重试朗读”，旧的朗读请求不会覆盖当前词条的错误提示。
- 导入取消、生词掌握切换和设置页不可操作项会向读屏用户报告当前禁用或勾选状态。
- 设置页开源项目入口在系统无法打开浏览器时会显示就地错误提示，不再让点击结果无反馈。
- 生词本单条“已掌握”操作在保存期间会锁定，避免快速连点把同一词条状态来回切换，并向读屏用户报告处理中状态。
- 书架编辑、管理、删除和生词移除确认弹层声明为模态视图，读屏用户会留在当前确认流程，不会误读背后的列表。
- 设置页字号边界会禁用无效的减小/增大操作，阅读目标、主题和英语音色会向读屏用户报告当前选中项。
- 设置页试听英语音色时会显示“试听中”并暂时锁定其它音色，避免快速切换造成重叠朗读或让用户误判最终选择。
- Piper 离线流式朗读发生生成错误时会进入系统音色回退并明确提示，阅读、复习和生词本不会把无声失败误认为已成功，也不会隐瞒可能使用系统音色的变化。
- 阅读器排版弹层的字号边界也会禁用无效操作，并向读屏用户报告当前不可用状态。
- 查词卡片、阅读排版和目录弹层的标题补充读屏标题语义，进入弹层后能快速定位当前内容。
- 切换到另一个单词时会清理上一个整句翻译请求的加载状态，旧请求返回后不会让新词卡永久显示加载中。
- 发现页和推荐详情的“想读”保存期间会锁定当前条目，并向读屏用户报告处理中状态，避免快速连点反复切换。
- 书籍和生词删除确认后会显示处理中并锁定取消/重复操作，正文清理或本地写入较慢时用户仍能确认操作正在进行。
- 删除书籍的本地写入失败时会保留当前书籍和确认上下文，在弹层内显示原因并提供“重试删除”，不必重新寻找书籍或误以为已删除。
- 删除书籍时会先保存生词和阅读记录，再发布移除后的书架索引；进程在中途退出也不会留下“生词指向书架外书籍”的启动阻断状态。
- 清除全部本地数据期间，启动页会明确显示正在清除并提醒保持应用打开，避免把高影响操作误认为普通的启动读取。
- 清除数据确认会说明用户导入内容与内置体验书的区别；重置后重新出现体验书不会被误解为个人书籍或学习记录未清除。
- 水平测试已有作答时，关闭按钮和系统返回会先询问，用户可以继续测试或明确放弃，避免首次使用过程中误丢进度。
- 水平测试未完成时会把答案草稿仅保存在本机，应用被系统回收或暂时退出后重新进入会回到第一道未答题；确认放弃、完成或重新开始会清理草稿。设备暂时无法写入时会就地提示，避免用户误以为退出后一定能恢复。
- 水平测试结果写入失败时，结果页仍明确显示当前会话结果并提供“重试保存”，只有等级真正落盘后才清除提示。
- 复习完成页会根据入口返回首页或生词本，完成复习后不会出现“返回生词本”与实际位置不一致的提示。
- 复习入口在没有到期词时会显示“现在没有到期词”，并引导继续阅读，不会把 0 个词误报为已完成的一轮复习。
- 从首页进入复习后跳回原文，会保留“返回今天”的上下文；阅读器错误、返回和读完提示不会把用户误带到生词本。
- 启动读取、正文读取、导入解析和 OCR 等失败路径都有可理解的恢复动作；核心弹层和按钮补充了读屏语义。阅读器、生词本和复习页的朗读失败会就地提示设备音量或英语音色检查，不阻塞后续操作。
- 书架编辑/删除和正文读取失败会统一隐藏原生英文异常，改为存储权限、文件不存在或稍后重试等中文恢复方向，避免用户被底层实现细节阻断。
- 随包离线词典初始化失败时，应用会降级到基础释义并继续进入书架、阅读和复习；设置与查词卡片会明确说明离线词典当前不可用，并提供设置页重试入口，避免词典模块故障阻断核心阅读。
- 离线词典运行中查询异常时也会自动降级到基础释义并标记为不可用，设置页随后提供重试入口，不会把一次数据库故障变成持续的查词失败。
- 词典查询异常后立即点击重试时会取消旧错误回调，避免旧请求在新加载开始后再次覆盖恢复状态。
- 正式安装包启动时，书架和阅读不再等待 SQLite 词典完成加载；加载期间查词使用基础兜底，词典就绪后自动切换到完整本地释义。
- 水平测试草稿恢复后，发现页会在用户再次进入时明确显示“继续水平测试”，并说明答案仍只保存在本机，避免用户误以为之前的作答已经丢失。
- 水平测试最后一道题也会先保存到本机草稿；若应用在结果保存前被系统回收，重新进入会识别完整草稿并直接恢复测试结果，不会要求用户重答最后一题。
- 扫描版 PDF OCR 在完成首批页面后会显示基于当前设备速度的剩余时间估计，用户能判断是否继续等待；估计会明确标注可能变化，不把预测当成承诺。
- 推荐详情遇到过期或无效的书籍 ID 时会显示可返回发现页的恢复状态，不再出现空白页面。
- 导入文件缺少有效文件名时会使用“未命名书籍”作为可识别的书架标题，避免导入成功后出现空标题。
- 文件选择器返回空资产时会显示明确的“未选择有效文件，请重试”，避免底层属性错误让用户无法判断下一步。
- 首页 Android 导入入口会列出完整的 TXT、EPUB、MOBI、AZW3、KF8 和 PDF 支持范围，避免把可导入的 Kindle KF8 文件误认为不受支持。
- 大型 EPUB/MOBI 导入的取消信号会在逐章解析边界生效，取消后不会继续创建书架条目，也不会把正常取消显示成导入错误。
- EPUB 导入读取大型导航目录时也会检查取消信号，用户点击停止后不会被目录扫描步骤长时间阻塞。
- MOBI/KF8 导入遍历大型目录时也会检查取消信号，减少停止导入的等待时间。
- MOBI/KF8 解析内部收到取消后会保留“导入已取消”信号，上层不会把用户主动停止误报为格式损坏。
- 导入正文文件写入失败时会清理已创建的孤立内容文件，避免书架索引尚未生成但设备空间已被占用。
- 首次创建体验书时若书架索引写入失败，也会清理已创建的体验书正文，启动重试不会累积孤立文件。
- 体验书索引即使在部分写入后才报告失败，也会尽力回滚旧书架，避免索引指向已经清理的正文。
- 恢复备份时，文件选择器以返回值或取消异常结束都会被视为正常取消，不再把用户主动退出选择误报成备份读取失败。
- PDF 原生读取失败会统一显示中文的文件访问、损坏或暂不支持提示，不再把 PDFBox 的内部英文错误直接暴露给用户；每类提示都给出重新选择、转换或重试方向。
- MOBI/AZW3/KF8 两种解析尝试都失败时会使用统一的格式与 DRM 排查提示，不再把底层 JavaScript 解析异常拼进导入失败信息。
- 首页、书架和推荐详情的文件选择器异常会统一显示权限、空间或文件失效提示；系统取消选择会安静返回，不会误报导入失败。
- 本地数据写入失败会把存储空间不足、设备繁忙和权限问题转换为中文恢复提示；未知底层错误不会直接把英文实现细节展示在全局重试横幅中。
- 导入恢复成功提示与本地保存失败提示同时出现时会自动错开，用户能同时看到“已恢复”与“仍需重试”的两种状态，不会因底部横幅重叠而漏掉数据风险。
- 备份导出、选择和恢复也会把文件提供器的空间、权限与文件失效异常转换为中文提示，用户能重新选择目录、检查权限或清理空间后继续。
- 备份恢复失败不会再以成功样式显示全局通知；恢复页面会保留原书架并显示可执行的失败提示，避免用户误以为数据已经替换。
- 设置页备份导出、读取或恢复失败会使用明显的错误语气和图标，成功提示保持中性说明，用户能一眼区分数据操作结果。
- 备份失败提示会直接提供对应的“重试导出”或“重试读取”入口，用户不必在长页面中重新寻找备份按钮。
- 备份恢复会校验书籍的章节数、总词数和当前位置是否与正文一致，损坏的进度元数据会在恢复前被拦截，不会把用户带到错误章节或显示错误总量。
- 编辑书名或作者后导出备份会用当前书架元数据同步正文副本，跨设备恢复时书架、正文和生词来源不会出现旧标题。
- 备份恢复还会校验生词返回原文的章节/段落位置、统计日期和阅读记录唯一性，损坏或重复的学习记录会在恢复前被拦截，避免点击“回到原文”或查看趋势时出现错位。

## 仍然需要用户验收的风险

### P0：正式 Android 安装包

需要在真实 Android 设备上验证随包 SQLite 词典、离线语音、文件选择器、PDF OCR、后台切换后的进度保存和备份恢复。Web 预览不能证明这些原生能力。

### P1：大文件和长时间阅读

需要用大章节 EPUB、长 TXT、复杂 MOBI 和多页扫描 PDF 验证导入耗时、内存占用、前后台切换和取消 OCR。当前代码有进度与取消路径，但还缺少真机性能基线。

### P1：Web 预览的查词预期

Web 现在内置约 10,000 个按词频筛选的轻量离线词典条目，关闭在线增强时未收录词仍使用本地基础兜底；正式安装包继续使用完整 SQLite 词典。后续仍需关注 Web 包体积和长尾词覆盖率。

### P2：长期学习反馈

目前首页已有今日分钟、连续天数、待掌握词、近 7 天阅读趋势，以及最近书籍的累计阅读和查词足迹；统计开关也已可用。下一阶段重点评估这些反馈能否帮助用户开始阅读或复习，再考虑按日期筛选的书籍回顾与更细粒度的统计控制。

## 下一阶段建议

1. **先做真机验收清单**：按 [ANDROID_ACCEPTANCE.md](ANDROID_ACCEPTANCE.md) 执行 Android 导入、离线查词、离线发音、OCR 取消、备份恢复和异常重启验收。
2. **优化大文件导入反馈**：显示已处理文件阶段、预计剩余时间和可安全取消的状态；对解析失败给出格式与修复建议。
3. **提升阅读连续性**：加入最近阅读历史和“继续阅读”快捷入口的稳定性检查，避免用户在多本书之间丢失上下文。
4. **让学习反馈更有行动性**：今日目标、到期复习词、最近查词和当前书籍已经集中成首页行动入口；后续继续观察真实使用后是否需要更主动的提醒。
5. **维护隐私信任**：每次新增联网能力都同时更新设置页、隐私文档和首次使用说明；在线增强继续保持主动开启。
6. **建立发布前可观测性**：在不上传正文和词义的前提下，记录本地解析失败类型和性能指标的匿名开关设计，先征得用户同意再启用。

## 验证边界

当前仓库已通过 TypeScript 类型检查、解析/分页/阅读进度/复习/备份恢复/翻译代理验证，并成功导出 Web 与 Android bundle。真实设备上的文件系统、SQLite、离线语音和 OCR 验收仍是发布前必做项。
